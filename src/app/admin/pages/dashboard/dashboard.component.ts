import { Component } from '@angular/core';

@Component({
  selector: 'app-dashboard',
  standalone: true,
  template: `
    <h1>Dashboard</h1>
    <p>Panel admin v2 — los CRUDs se migran aquí con la tabla y modal genéricos (Etapas 2-3).</p>
  `
})
export class DashboardComponent {}
